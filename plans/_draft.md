# _draft — borrador vivo QRTE

Lo escribe y reescribe el `planner` hasta aprobación. No se versiona.

Al aprobar se congela a `YYYY-MM-DD-<track>-<slug>.md` y no se edita.
Cambios posteriores van en `<mismo-nombre>-delta-NN.md`.

Política: `Quick` 1 archivo sin migraciones no se congela, queda 1 línea si aportó.
Máximo 5 `Feature/System` vigentes, al sexto se archiva o borra el más viejo con confirmación.
