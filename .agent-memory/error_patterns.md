# Error patterns — QRTE

Fallos ya vistos y cómo se arreglaron. Solo repetibles con fix verificado.
Política: `Quick` y un solo uso no suben, quedan en el plan. Obsoleto se marca, no se borra.

## Formato

- Error — síntoma — fix verificado.

## Patrones

- (vacío, primer patrón lo propone `developer` y lo confirma `documenter`)
